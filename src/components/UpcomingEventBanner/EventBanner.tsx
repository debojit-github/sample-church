import React from 'react';
import { TopBannerContainer } from './styled';

const EventBanner: React.FunctionComponent<{}> = ({ }) => (
  <TopBannerContainer>
    Something
  </TopBannerContainer>
);

export default EventBanner;